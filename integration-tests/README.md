# Integration Tests

## Requirements
- Python 3.7+
- Gradle (already included via gradlew)

## How to Run
From project root:

```bash
cd integration-tests
python test_pricing.py
```

## What is Tested
- Project structure (required files)
- Gradle build success
- All JUnit tests pass
- Configuration files (.gitignore, README.md)